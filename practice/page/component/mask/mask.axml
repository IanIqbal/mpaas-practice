<mask type="{{type}}" show="{{show}}" maskZindex="{{maskZindex}}" onMaskTap="maskClick"></mask>
<view a:if="{{type === 'market'}}">Marketing popup overlay，type="{{type}}"</view>
<view a:if="{{type === 'product'}}">Product popup overlay，type="{{type}}"</view>